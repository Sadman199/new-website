@extends('front.layout.app')

@section('main_content')
<div class="page-top">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <nav class="breadcrumb-container">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="{{ route('home') }}">Home</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Forex Deposit Bonus</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="page-content">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h2 class="section_title">Deposit Bonus</h2>
            </div>
            @foreach ($forexBonuses as $bonus)
            <div class="col-md-3 mb-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title">{{ $bonus->title }}</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Author:</strong> {{ $bonus->author_name }}</p>
                        <p><strong>Publish Date:</strong> {{ $bonus->publish_date }}</p>
                        <p><strong>Bonus Type:</strong> {{ $bonus->promo_type }}</p>
                        <p>{{ Str::limit(strip_tags($bonus->description), 100) }}</p>


                        @if ($bonus->feature_image)
                        <img src="{{ Storage::url($bonus->feature_image) }}" class="custom-img" alt="Feature Image">
                        @else
                        <p>No image available.</p>
                        @endif
                        <a href="{{ $bonus->link }}" target="_blank" class="btn btn-primary">View Details</a>
                    </div>
                </div>
            </div>
            @endforeach
        </div>
    </div>
</div>
@endsection