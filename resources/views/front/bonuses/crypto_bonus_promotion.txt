@extends('front.layout.app')

@section('main_content')
<div class="page-top">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h2>Crypto Bonus Promotion</h2>
                <nav class="breadcrumb-container">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="{{ route('home') }}">Home</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Crypto Bonus Promotion</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="page-content">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <!-- Check if there are any Crypto Bonus Promotions -->
                @if($cryptoBonuses->isEmpty())
                    <div class="alert alert-warning" role="alert">
                        No Crypto Bonus Promotions available at the moment.
                    </div>
                @else
                    <!-- Displaying crypto bonus promotion list -->
                    <ul class="list-group">
                        @foreach($cryptoBonuses as $bonus)
                            @if($bonus->promo_type === 'Crypto Bonus Promotion')
                                <li class="list-group-item">
                                    <h4>{{ $bonus->name }}</h4>
                                    <p>{{ $bonus->description }}</p>
                                    <p><strong>Deposit Method:</strong> {{ $bonus->deposit_method }}</p>
                                    <p><strong>Promotion Details:</strong> {{ $bonus->bonus_details }}</p>
                                </li>
                            @endif
                        @endforeach
                    </ul>

                    <!-- Pagination links -->
                    {{ $cryptoBonuses->links() }}
                @endif
            </div>
        </div>
    </div>
</div>
@endsection
